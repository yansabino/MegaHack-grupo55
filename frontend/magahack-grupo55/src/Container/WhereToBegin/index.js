import React from "react";
import Header from "../../components/header/index";

export default function WhereToBegin(props) {
  return (
    <div>
      <Header history={props.history} />
    </div>
  );
}
